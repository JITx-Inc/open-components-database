#use-added-syntax(tests)
defpackage ocdb/test/landpattern-generators:
  import core
  import collections
  import jitx
  import jitx/commands
  import ocdb/landpatterns

deftest bga-pad-names:
  val refs = for ch in "ABCDEFGHJKLMNPRSTUVWY"

  #EXPECT(bga-pad-name(, 4) == IndexRef("A", ))

deftest full-matrix-bga:

deftest staggered-matrix-bga:
  pcb-landpattern WLCSP18:
    make-bga-landpattern(
      5, 7, 
      0.40, 0.25, 
      tol(1.86, 0.02), tol(2.14, 0.02), 
      StaggeredMatrix()
    )
  