#use-added-syntax(tests, jitx)
defpackage ocdb/test/naming-conventions:
  import core
  import collections
  import ocdb/land-patterns
  import jitx

deftest test-bga-naming:
  #EXPECT(bga-pin-name(0, 0) == IndexRef(Ref("A"), 1))
  #EXPECT(bga-pin-name(8, 6) == IndexRef(Ref("J"), 7))
  #EXPECT(bga-pin-name(20, 4) == IndexRef(Ref("AA"), 5))
  #EXPECT(bga-pin-name(21, 3) == IndexRef(Ref("AB"), 4))
